import { styled, Box } from "@mui/material";

export const TextContentWrapper = styled(Box)({
  flexGrow: 1,
  paddingTop: "1rem",
  paddingBottom: "1rem",
  paddingLeft: "1rem",
  paddingRight: "1rem",
});
